[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "zyra-network"
version = "1.0.93"
description = "A local agentic AI network that earns Proof of Useful Work (PoUW)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "PySide6>=6.5.0",
    "PyYAML>=6.0",
    "torch>=2.0.0",
    "psutil>=5.9.0",
    "numpy>=1.24.0",
    "httpx",
    "openai",
    "pyautogui",
    "duckduckgo-search",
    "web3>=6.0.0"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pyinstaller"
]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*", "ai*", "zyra_cmd*"]
[project.scripts]
zyra = "zyra_cmd.zyra_cli:main"
